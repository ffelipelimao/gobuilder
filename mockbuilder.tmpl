package mockbuilder 

type {{.Name}}MockBuilder struct {
    mock entities.{{.Name}}
} 

func {{.Name}}() *{{.Name}}MockBuilder{
    return &{{.Name}}MockBuilder{
        mock: Mock{{.Name}},
    }
}

func (b *{{.Name}}MockBuilder) Build() entities.{{.Name}} {
	return b.mock
}

{{ range $key, $element := .Fields }}

func (b *{{$.Name}}MockBuilder) With{{ $key }}(value {{ $element }}) entities.{{$.Name}} {
	b.mock.{{ $key }} = value
    return b.mock
}

{{end -}} 
